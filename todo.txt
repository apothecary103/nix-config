x 2026-08-03 baseline: 5413 lines of .nix (635 comment, 68 files) + 1814 lines of .qml +metrics
x 2026-08-03 verified: home.shell.enableShellIntegration defaults true; catppuccin.sources.palette ships palette.json; none of the 6 casks exist for aarch64-darwin in nixpkgs +metrics

x 2026-08-04 reverted: hyprland.nix's bind/env/curve/anim helper layer went back to literal `_args` entries — the generated hyprland.lua is byte-identical modulo bind order, and the literal form is the one you can read +loc @linux file:modules/desktop/hyprland.nix
x 2026-08-04 reverted: hyprland's 17 disabled animation leaves are spelled out again, with the fold gone +loc @linux file:modules/desktop/hyprland.nix
(A) 2026-08-03 Decide whether Hyprland stays at all — it is a hand-transcribed clone of niri.nix and 25% of the whole repo; dropping it also removes the greetd session entry, the wezterm/hyprland-run window rules and the compositor-parity comments +loc @linux est:1400 file:modules/desktop/hyprland.nix
x 2026-08-04 Replace theme.nix's two hardcoded 26-colour palettes with lib.mapAttrs (_: c: c.hex) (lib.importJSON "${sources.palette}/palette.json").${flavor}.colors — catppuccin/nix already ships the palette port, same way its own tty.nix consumes it +loc @nix est:55 file:modules/desktop/theme.nix
x 2026-08-04 Delete every enableFishIntegration/enableNushellIntegration/enableBash/Zsh = true — home.shell.enableShellIntegration already defaults them all to true; set the one global if a shell ever needs opting out +loc @nix est:11 file:modules/programs/{net/pass,shell/yazi,shell/eza,shell/zoxide,shell/nushell}.nix

x 2026-08-04 reverted: skhd's 1-9 and h/j/k/l blocks are 22 literal lines again, not two string-generating helpers +loc @darwin file:modules/desktop/yabai.nix
(B) 2026-08-03 Generate yabai's four *_padding attrs from one value +loc @darwin est:3 file:modules/desktop/yabai.nix
x 2026-08-04 Delete the commented-out Zed One Dark palette in ghostty.nix — dead code, catppuccin drives the theme +loc est:27 file:modules/programs/term/ghostty.nix
x 2026-08-04 Delete the commented-out enableFishIntegration line in fzf.nix +loc est:1 file:modules/programs/shell/fzf.nix
x 2026-08-04 reverted: files.nix lists its 9 image/* mimeApps entries literally again +loc @linux file:modules/desktop/files.nix
x 2026-08-04 reverted: rmpc's label/field/column helpers removed, header rows and song_table_format written out +loc file:modules/programs/media/rmpc.nix
x 2026-08-04 Merge bat.nix's MANPAGER/MANROFFOPT into man.nix or vice versa — one file owns man rendering +loc est:8 file:modules/programs/shell/{bat,man}.nix
x 2026-08-04 reverted: forEachOs/forBothOs deleted from parts.nix; nix.nix and fonts.nix are back to a let-bound body assigned to both classes, nix-index.nix to two explicit blocks +dedup @nix file:modules/flake/parts.nix
x 2026-08-04 reverted: session.nix deleted, terminal/menu inlined in niri.nix (hyprland's literal form carries its own) +dedup @linux
x 2026-08-04 Derive nh.flake from username + platform home dir instead of writing /home/… and /Users/… out twice +dedup est:6 file:modules/system/nh.nix

x 2026-08-04 de-clever pass: metaprogramming introduced above rolled back to plain declarations on your call. Kept: the palette import, every deletion (wezterm, teamspeak, dead ghostty/fzf comments, redundant shell-integration flags), the bat/man merge, nh's derived flake path, the homebrew exit, MonoText.qml

x 2026-08-04 Dropped wezterm.nix; foot.nix kept on your call. niri's wezterm initial-configure window rule went with it +loc est:50 file:modules/programs/term/wezterm.nix
(C) 2026-08-03 Drop zsh.nix if fish is the login shell and nushell is the alternate — three configured shells is two more than are used +loc est:11 file:modules/programs/shell/zsh.nix
(C) 2026-08-03 Decide between helix (defaultEditor, steelix + oil/glyph cogs) and nvf; keeping both costs a 113-line neovim config that nothing points at +loc est:113 file:modules/programs/editor/neovim.nix
(C) 2026-08-03 Audit structural-narration comments the AGENTS.md rule already forbids: "# Font configuration", "# CLI Tools", "# Focus Navigation (Arrows)", "# Focus spaces 1 through 9 using Alt + Number", the ---- SECTION ---- banners in hyprland/niri/rmpc +loc est:60
(C) 2026-08-03 Consider one shell/tools.nix for the trivially-short single-option modules (direnv, zoxide, eza, fzf, man, zsh) — saves ~3 lines of module wrapper each, at the cost of dendritic granularity; judgement call, not a clear win +loc est:20

x 2026-08-07 reverted: the whole homebrew exit is off on your call — _dmg-app.nix and dmg-apps.nix deleted, every app back to a cask. The dmg repack worked, but a hash and a version to bump per app, plus TCC re-grants on every store-path bump, is more upkeep than the cask list it replaced +homebrew-exit @darwin file:modules/system/homebrew.nix
x 2026-08-07 teamspeak6 follows the same exit: _teamspeak6-client.nix stays deleted, the teamspeak-client@beta cask takes over. Settings and login live in ~/Library/Application Support, so neither moves with the bundle +homebrew-exit @darwin
(B) 2026-08-07 The teamspeak-client@beta cask pins 6.0.0-beta3.4, older than the beta4.1 the derivation had, and carries `disable! date: "2026-09-01", because: :fails_gatekeeper_check` — decide before September whether to pin the tap, go unmanaged, or take TS3 +homebrew-exit @darwin
x 2026-08-07 dropped vorssaint on your call; homerow and whichspace stay dropped too +homebrew-exit @darwin

(C) 2026-08-03 Ambitious: drive niri.nix and hyprland.nix from one shared bind table of { keys, niriAction, hyprlandLua } and render both — the real fix for the duplication if both compositors stay, but it makes each individual bind harder to read +dedup @linux est:200
(C) 2026-08-03 Check whether _yaagl.nix (196 lines) can be reduced to an override of anything in nixpkgs, or upstreamed +loc @linux est:150 file:modules/programs/games/_yaagl.nix
x 2026-08-04 Sweep the 1814 lines of QML for the same kind of repetition once the .nix side is done — it is a third of the repo and nothing above touches it +loc @linux

x 2026-08-04 Per change: nix fmt, then nix eval .#darwinConfigurations.fern.system.drvPath and nix eval 'path:.#nixosConfigurations.frieren.config.<attr>' — frieren's toplevel cannot eval on the Mac (catppuccin librewolf IFD needs an aarch64-linux builder) +process
(A) 2026-08-03 One concern per commit; the working tree already mixes 9 unrelated modified files, stage them apart before starting any of this +process
(B) 2026-08-04 Optional homebrew shrink: the homebrew-core tap input is only needed for formulae and there are none left, so only homebrew-cask is load-bearing. Untested — dropping it changes what `brew update` sees at activation, which needs a real darwin-rebuild to confirm +homebrew-exit @darwin est:5
(C) 2026-08-04 QML: an IconButton component would fold the repeated `Item { 16x16; verticalCenter } + MouseArea { PointingHandCursor }` in Tray/NowPlaying/Volume, and a Behavior-subclass would fold the six `NumberAnimation { OutCirc }` blocks. Left undone: cannot run the Wayland shell from the Mac to see the result +loc @linux est:45
