# ty isn't in helix's built-in server list yet, so define it explicitly.
[language-server.ty]
command = "ty"
args = ["server"]

[[language]]
name = "python"
auto-format = true
language-servers = ["ty", "ruff"]
